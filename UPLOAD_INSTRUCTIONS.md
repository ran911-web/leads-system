# העלאה לריפו ran911-web/leads-system

1. חלץ את ה-ZIP.
2. ב-GitHub: **Add file → Upload files** → גרור את **כל תוכן** התיקייה שחולצה (index.html, sw.js, package.json, supabase/) → **Commit changes**.
   GitHub שומר את מבנה התיקיות, ומחליף קבצים קיימים באותו שם.
3. אחרי ההעלאה: בטלפון — סגור ופתח את האפליקציה פעם אחת.

**המסד כבר עודכן** (אין מה להריץ). קובצי ה-SQL בתיקייה `supabase/migrations/` הם תיעוד + שחזור סביבת בדיקה.
